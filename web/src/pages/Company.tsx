import React from "react";

export interface ICompanyPageProps{}

const Company: React.FunctionComponent<ICompanyPageProps> = props => {
    return(
    <div>
        <p>Essa é a Pagina de empressa</p>
    </div>
    );
};

export default Company;