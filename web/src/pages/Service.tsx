import React from "react";

export interface IServicePageProps{}

const Service: React.FunctionComponent<IServicePageProps> = props => {
    return(
    <div>
        <p>Essa é a Pagina de Service</p>
    </div>
    );
};

export default Service;